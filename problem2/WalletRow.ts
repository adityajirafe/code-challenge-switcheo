const WalletRow = React.memo(({ className, amount, usdValue, formattedAmount  }) => {
  // Rendering logic here
  return (
    <div>
      {/* Render balance information */}
    </div>
  );
});

export default WalletRow;